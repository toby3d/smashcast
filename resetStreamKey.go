package hitGox

/*
import (
	"bytes"
	"encoding/json"
	"fmt"
	"github.com/valyala/fasthttp"
)

// ResetStreamKey get the stream key for channel.
func ResetStreamKey(channel string, authToken AuthToken) (StreamKey, error) {

}
*/
